# Проверочная работа по React Router v6
## Тема: Маршрутизация, навигация, вложенные маршруты, защищённые маршруты
### Время выполнения: 60 минут
### Максимальный балл: 100

---

## Инструкция для студента

- Внимательно прочитайте каждый вопрос
- Время выполнения — 60 минут
- Работа состоит из 6 разделов
- Разрешено использование ваших предыдущих проектов как справочного материала
- Пишите код разборчиво

---

## Раздел 1: Теоретические вопросы (15 баллов, 3 вопроса × 5 баллов)

### Вопрос 1 (5 баллов)
Что такое `Outlet` в React Router? Для чего он используется? Приведите пример использования.

**Место для ответа:**
```
Outlet - это _компонент из React Router, который работает  как место-заполнитель для рендера дочерних маршрутов. Он указывает , где именно внутри родительского компонента ждолдны отображаться компоненты, соответствующие вложенным маршрутам. 

Используется для: Используется для общих компонентов, которые остаются неизменными, в то время, как основное содержимое меняется в зависимости от маршрута.

Пример:
Layout компоненты: шапка, подвал, боковая панель
```

---

### Вопрос 2 (5 баллов)
Объясните разницу между **абсолютными** и **относительными** ссылками в React Router. Приведите примеры.

**Место для ответа:**
```
Абсолютные ссылки: начинаются с символа '/' и всегда ведут от корня сайта, независимо от текущего URL адреса. Они игнорируют текущий путь и ссылаются на точный адрес.
Пример: <Link to="/products/123/reviews">Отзывы</Link> При любом текущем URL ведет на /products/123/reviews

Относительные ссылки: _______________________________________
Пример: <Link to="reviews">Отзывы</Link>

Когда какую использовать: __________________________________
```

---

### Вопрос 3 (5 баллов)
Что такое вложенные маршруты (nested routes)? Как их создать? Какой компонент используется для их отображения?

**Место для ответа:**
```
Вложенные маршруты - это ____________________________________

Для создания нужно: _________________________________________

Компонент для отображения: _________________________________

Синтаксис:
___________________________________________________________
___________________________________________________________
```

---

## Раздел 2: Найди ошибку (15 баллов, 3 задания × 5 баллов)

### Задание 1 (5 баллов)
Найдите **3 ошибки** в следующем коде:

```jsx
// App.jsx
import { BrowserRouter, Routes, Route } from 'react-router-dom';
import { Layout } from './Layout';
import { Home } from './Home';
import { About } from './About';

function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/" component={Layout}>
          <Route index component={Home} />
          <Route path="/about" component={About} />
        </Route>
      </Routes>
    </BrowserRouter>
  );
}
```

**Ошибка 1:** _________________________________________________

**Ошибка 2:** _________________________________________________

**Ошибка 3:** _________________________________________________

---

### Задание 2 (5 баллов)
Найдите ошибку в компоненте:

```jsx
function UserProfile() {
  const { userId } = useParams();
  const navigate = useNavigate();
  
  const handleSave = () => {
    saveUserData(userId);
    navigate(-1);
  };
  
  return (
    <div>
      <h1>Профиль пользователя {userId}</h1>
      <button onClick={handleSave}>Сохранить</button>
      <Link to="./edit">Редактировать</Link>
    </div>
  );
}
```

**Ошибка:** _________________________________________________

**Почему это ошибка:** _______________________________________

**Исправление:** ____________________________________________

---

### Задание 3 (5 баллов)
Найдите ошибку в настройке защищённого маршрута:

```jsx
function PrivateRoute({ children }) {
  const isAuth = useSelector(state => state.auth.isAuth);
  
  if (!isAuth) {
    return <Redirect to="/login" />;
  }
  
  return children;
}

// Использование:
<Routes>
  <Route path="/dashboard" element={
    <PrivateRoute>
      <Dashboard />
    </PrivateRoute>
  } />
</Routes>
```

**Ошибка:** _________________________________________________

**Как правильно в React Router v6:** _________________________

---

## Раздел 3: Написание кода (35 баллов)

### Задание 1: Создание Layout с навигацией (10 баллов)

Напишите компонент `Layout`, который содержит:
- Шапку с навигацией (ссылки: Главная, О нас, Товары, Контакты)
- Область для отображения дочерних маршрутов (используя Outlet)
- Подвал с копирайтом

```jsx
// Layout.jsx
import { /* нужные импорты */ } from 'react-router-dom';

export function Layout() {
  return (
    <div>
      <header>
        <nav>
          {/* Напишите ссылки здесь (5 баллов) */}
        </nav>
      </header>
      
      <main>
        {/* Что здесь должно быть? (3 балла) */}
      </main>
      
      <footer>
        {/* Подвал (2 балла) */}
      </footer>
    </div>
  );
}
```

---

### Задание 2: Настройка маршрутов (15 баллов)

Настройте маршруты в `App.jsx` согласно требованиям:

**Требования:**
1. Корневой маршрут `/` использует `Layout` и отображает `HomePage`
2. Маршрут `/about` отображает `AboutPage`
3. Маршрут `/products` отображает `ProductsPage`
4. Маршрут `/products/:id` отображает `ProductDetail` (вложенный в ProductsPage)
5. Маршрут `/products/:id/reviews` отображает `ProductReviews` (вложенный)
6. Маршрут `/dashboard` защищённый, требует авторизации
7. Все несуществующие маршруты ведут на `NotFound`

```jsx
// App.jsx
import { BrowserRouter, Routes, Route } from 'react-router-dom';
import { Layout } from './components/Layout';
import { PrivateRoute } from './components/PrivateRoute';
import { HomePage } from './pages/HomePage';
import { AboutPage } from './pages/AboutPage';
import { ProductsPage } from './modules/products/ProductsPage';
import { ProductDetail } from './modules/products/ProductDetail';
import { ProductReviews } from './modules/products/ProductReviews';
import { Dashboard } from './pages/Dashboard';
import { NotFound } from './pages/NotFound';
import { Login } from './pages/Login';

function App() {
  return (
    <BrowserRouter>
      <Routes>
        {/* Напишите маршруты здесь (по 2 балла за каждый правильный маршрут) */}
        
      </Routes>
    </BrowserRouter>
  );
}

export default App;
```

---

### Задание 3: Компонент ProductsPage с вложенными маршрутами (10 баллов)

Напишите компонент `ProductsPage`, который:
- Имеет боковую панель со ссылками на категории
- Использует `Outlet` для отображения вложенных маршрутов
- Имеет заголовок "Каталог товаров"
- Ссылки на категории: "Все", "Электроника", "Одежда"

```jsx
// ProductsPage.jsx
import { /* нужные импорты */ } from 'react-router-dom';

export function ProductsPage() {
  return (
    <div>
      <h1>/* Заголовок */</h1>
      
      <div style={{ display: 'flex', gap: '20px' }}>
        <aside style={{ width: '200px' }}>
          <h3>Категории</h3>
          <nav>
            {/* Напишите ссылки на категории (5 баллов) */}
          </nav>
        </aside>
        
        <main style={{ flex: 1 }}>
          {/* Что должно быть здесь? (5 баллов) */}
        </main>
      </div>
    </div>
  );
}
```

---

## Раздел 4: Работа с хуками (15 баллов)

### Задание 1 (5 баллов)
Напишите компонент, который извлекает параметр `id` из URL и отображает его на экране.

```jsx
function UserPage() {
  // Напишите код здесь
  // Используйте хук useParams
  
  return (
    <div>
      <h1>Профиль пользователя</h1>
      <p>ID пользователя: {/* выведите id здесь */}</p>
    </div>
  );
}
```

---

### Задание 2 (5 баллов)
Напишите компонент кнопки "Назад", которая возвращает пользователя на предыдущую страницу.

```jsx
function BackButton() {
  // Напишите код здесь
  // Используйте хук useNavigate
  
  return (
    <button>
      ← Назад
    </button>
  );
}
```

---

### Задание 3 (5 баллов)
Напишите компонент, который отображает текущий путь (pathname) и может добавлять GET-параметр `?page=2`.

```jsx
function CurrentPath() {
  // Напишите код здесь
  // Используйте хуки useLocation и useSearchParams
  
  return (
    <div>
      <p>Текущий путь: {/* pathname */}</p>
      <button>
        Перейти на страницу 2
      </button>
    </div>
  );
}
```

---

## Раздел 5: Защищённые маршруты (10 баллов)

### Задание (10 баллов)
Напишите компонент `PrivateRoute`, который:
- Проверяет авторизацию через Redux store (поле `isAuthenticated`)
- Если пользователь авторизован — отображает дочерние маршруты (через `Outlet`)
- Если не авторизован — перенаправляет на страницу `/login`
- Показывает индикатор загрузки, пока проверяется статус авторизации

```jsx
// PrivateRoute.jsx
import { /* нужные импорты */ } from 'react-router-dom';
import { useSelector } from 'react-redux';

export function PrivateRoute() {
  // Напишите код здесь (10 баллов)
  
  // 1. Получите статус авторизации из Redux (3 балла)
  // 2. Получите статус загрузки (2 балла)
  // 3. Покажите индикатор загрузки, если идёт проверка (2 балла)
  // 4. Перенаправьте на /login, если не авторизован (3 балла)
}
```

---

## Раздел 6: Диагностика и исправление (10 баллов)

### Задание (10 баллов)
Дан компонент со страницей товара. Найдите и исправьте **все ошибки** (минимум 4):

```jsx
import { useParam, Link, useNavigate } from 'react-router-dom';
import { useDispatch, useSelector } from 'react-redux';

function ProductPage() {
  const id = useParam();
  const dispatch = useDispatch();
  const navigate = useNavigate();
  const product = useSelector(state => state.products[id]);
  
  useEffect(() => {
    dispatch(fetchProduct(id));
  });
  
  const handleDelete = () => {
    dispatch(deleteProduct(id));
    navigate('/products');
  };
  
  return (
    <div>
      <h1>{product.name}</h1>
      <p>{product.description}</p>
      <button onClick={handleDelete}>Удалить</button>
      <Link to="edit">Редактировать</Link>
      <Link to="/products">Назад</Link>
    </div>
  );
}
```

**Список ошибок:**

1. _________________________________________________

2. _________________________________________________

3. _________________________________________________

4. _________________________________________________

**Исправленный код:**

```jsx
// Напишите исправленную версию здесь
```

---
