# Проверочная контрольная работа по Redux Toolkit
## Время выполнения: 60 минут
## Максимальный балл: 100

---

## Инструкция для студента

- Внимательно прочитайте каждый вопрос
- Время выполнения — 60 минут
- Работа состоит из 5 разделов
- Запрещено использование Google/учебников (проверка знаний)
- Разрешено использование вашего предыдущего кода Todo-приложения как справочного материала
- Пишите код разборчиво, поясняйте ключевые моменты

---

## Раздел 1: Теоретические вопросы (20 баллов, 4 вопроса × 5 баллов)

### Вопрос 1 (5 баллов)
Что делает функция `configureStore()` и в чём её преимущество перед `createStore()`? Перечислите минимум 3 преимущества.

**Место для ответа:**
```
1. _________________________________________________
2. _________________________________________________
3. _________________________________________________
```

---

### Вопрос 2 (5 баллов)
Объясните разницу между `useSelector` и `useDispatch`. В каких ситуациях используется каждый из них? Приведите пример кода для каждого.

**Место для ответа:**
```
useSelector: ________________________________________
Пример: ___________________________________________

useDispatch: _______________________________________
Пример: ___________________________________________
```

---

### Вопрос 3 (5 баллов)
Что такое "слайс" (slice) в Redux Toolkit? Какие три основных поля он содержит? Зачем нужен `createSlice()`?

**Место для ответа:**
```
Слайс - это ________________________________________

Три основных поля:
1. _________________________________________________
2. _________________________________________________
3. _________________________________________________

createSlice() нужен для: ___________________________
```

---

### Вопрос 4 (5 баллов)
Что произойдёт, если вызвать `dispatch` напрямую в теле компонента (не в `useEffect` и не в обработчике события)? Почему это плохо? Как правильно загружать данные при монтировании компонента?

**Место для ответа:**
```
Что произойдёт: ____________________________________

Почему плохо: ______________________________________

Как правильно: _____________________________________
```

---

## Раздел 2: Найди ошибку (15 баллов, 3 ошибки × 5 баллов)

### Задание 1 (5 баллов)
Найдите **3 ошибки** в следующем коде:

```typescript
// counterSlice.ts
import { createSlice } from '@reduxjs/toolkit';

const counterSlice = createSlice({
  initialState: 0,
  name: 'counter',
  reducer: {
    increment: (state) => state + 1,
    decrement: (state) => state - 1
  }
});

export const { increment, decrement } = counterSlice.actions;
export default counterSlice.reducers;
```

**Ошибка 1:** _________________________________________________

**Ошибка 2:** _________________________________________________

**Ошибка 3:** _________________________________________________

---

### Задание 2 (5 баллов)
Найдите ошибку в компоненте:

```tsx
function UserProfile() {
  const dispatch = useDispatch();
  const user = useSelector(state => state.user);
  
  dispatch(fetchUser(user.id));
  
  return <div>{user.name}</div>;
}
```

**Ошибка:** _________________________________________________

**Исправление:** _____________________________________________

---

### Задание 3 (5 баллов)
Найдите ошибку в конфигурации store:

```typescript
import { configureStore } from '@reduxjs/toolkit';
import todosReducer from './todosSlice';

const store = configureStore({
  reducer: todosReducer
});

export type RootState = typeof store.getState;
export type AppDispatch = typeof store.dispatch;
```

**Ошибка:** _________________________________________________

**Почему это проблема:** _____________________________________

---

## Раздел 3: Написание кода (35 баллов)

### Задание 1: Создание слайса (15 баллов)
Создайте Redux Toolkit slice для управления корзиной покупок (shopping cart). Требования:

- Начальное состояние: `{ items: [], totalPrice: 0 }`
- Тип для товара: `{ id: number; name: string; price: number; quantity: number }`
- Редюсеры:
  - `addItem` — добавляет товар в корзину (если товар уже есть, увеличивает quantity)
  - `removeItem` — удаляет товар из корзины
  - `updateQuantity` — изменяет количество товара
  - `clearCart` — очищает всю корзину
- Автоматически пересчитывать `totalPrice` при любом изменении

**Напишите код здесь:**

```typescript
// cartSlice.ts

// 1. Определите типы (2 балла)
interface CartItem {
  // Дополните код
}

interface CartState {
  // Дополните код
}

// 2. Начальное состояние (2 балла)
const initialState: CartState = {
  // Дополните код
};

// 3. Создайте слайс (11 баллов)
const cartSlice = createSlice({
  // Дополните код
});

// 4. Экспорты
export const { /* ... */ } = cartSlice.actions;
export default cartSlice.reducer;
```

---

### Задание 2: Настройка store (10 баллов)
Настройте Redux store для приложения, которое использует два слайса: `cart` (из задания выше) и `user` (управление пользователем).

```typescript
// store.ts

import { configureStore } from '@reduxjs/toolkit';
// Импортируйте редюсеры

export const store = configureStore({
  // Дополните код (3 балла)
});

// Экспортируйте типы RootState и AppDispatch (2 балла)
export type RootState = /* ... */;
export type AppDispatch = /* ... */;

// Создайте типизированные хуки (3 балла)
export const useAppDispatch = /* ... */;
export const useAppSelector: /* ... */ = /* ... */;
```

---

### Задание 3: Использование в компоненте (10 баллов)
Напишите компонент `Cart`, который:
- Отображает список товаров в корзине (используя `useSelector`)
- Показывает общую стоимость
- Имеет кнопку "Очистить корзину"
- Для каждого товара показывает кнопки "+"/"-" для изменения количества и кнопку "Удалить"

```tsx
// Cart.tsx

import React from 'react';
// Добавьте необходимые импорты

export const Cart: React.FC = () => {
  // Получите данные из store (3 балла)
  
  // Получите dispatch (1 балл)
  
  // Обработчики событий (3 балла)
  
  return (
    <div>
      {/* JSX разметка (3 балла) */}
    </div>
  );
};
```

---

## Раздел 4: Теоретические задачи (20 баллов)

### Задача 1 (5 баллов)
Объясните, что такое "неизменяемость" (immutability) в Redux. Почему в Redux Toolkit можно писать `state.push(item)`, хотя это выглядит как мутация?

**Ответ:** _________________________________________________

_____________________________________________________________
_____________________________________________________________
_____________________________________________________________

---

### Задача 2 (5 баллов)
У вас есть слайс для постов. Напишите селектор, который возвращает только опубликованные посты (поле `isPublished: true`), отсортированные по дате (поле `createdAt`) от новых к старым.

```typescript
// Напишите селектор
export const selectPublishedPosts = (state: RootState) => {
  // Ваш код
};
```

---

### Задача 3 (5 баллов)
В чём разница между `extraReducers` и обычными `reducers` в `createSlice()`? Когда нужно использовать `extraReducers`?

**Ответ:** _________________________________________________

_____________________________________________________________
_____________________________________________________________
_____________________________________________________________

---

### Задача 4 (5 баллов)
Что такое "типизированные хуки" (`useAppDispatch`, `useAppSelector`)? Зачем их создавать в проекте с TypeScript?

**Ответ:** _________________________________________________

_____________________________________________________________
_____________________________________________________________
_____________________________________________________________

---

## Раздел 5: Отладка и рефакторинг (10 баллов)

### Задание (10 баллов)
Дан следующий код с проблемами производительности. Найдите и исправьте все проблемы:

```tsx
function ProductList() {
  const dispatch = useDispatch();
  const products = useSelector(state => state.products.items);
  const filters = useSelector(state => state.filters);
  
  const filteredProducts = products.filter(product => {
    if (filters.category && product.category !== filters.category) return false;
    if (filters.price && product.price > filters.price) return false;
    return true;
  });
  
  useEffect(() => {
    dispatch(fetchProducts());
  });
  
  return (
    <div>
      {filteredProducts.map(product => (
        <ProductCard key={product.id} product={product} />
      ))}
    </div>
  );
}
```

**Список проблем (минимум 3):**

1. _________________________________________________

2. _________________________________________________

3. _________________________________________________

4. (дополнительно) __________________________________

**Исправленный код:**

```tsx
// Напишите исправленную версию
```

---


**Желаю успеха! У вас есть 60 минут. Работайте внимательно и не торопитесь.** Результаты направьте, пожалуйста, в чат.